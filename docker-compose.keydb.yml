services:
  keydb:
    image: eqalpha/keydb
    container_name: keydb
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - keydb_data:/data
    command: keydb-server --requirepass <YOUR_KEYDB_PASSWORD>

volumes:
  keydb_data: